import 'package:flutter/material.dart';

class Plenka extends StatelessWidget {
  const Plenka({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),

      body: SingleChildScrollView(),
    );
  }
}
