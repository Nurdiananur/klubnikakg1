import 'package:flutter/material.dart';

class Kocottor extends StatelessWidget {
  const Kocottor({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: SingleChildScrollView(
        child:Column(

        ),
      ),
    );
  }
}
